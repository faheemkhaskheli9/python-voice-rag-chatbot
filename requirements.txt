# Runtime dependencies (Phase 1: microphone input capture).
# Pinned as implementation progresses.

SpeechRecognition>=3.10,<4    # microphone interface + AudioData container

# Live microphone capture also needs a PyAudio backend. It is intentionally
# NOT listed above: it needs the system PortAudio library, is not needed for
# the file-based capture path, and CI has no audio device. Install locally
# with:  pip install PyAudio>=0.2.11
